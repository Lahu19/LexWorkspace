# ─── Backend API (Express + MongoDB) ───────────────────────────────────────
# The Express server URL — used for both REST API calls and LLM streaming.
VITE_API_URL=http://localhost:3001

# ─── Legacy (kept to avoid vite build warnings, values unused) ─────────────
# These Supabase variables are no longer used by the application.
# You can delete them once Google Auth is wired up.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_PUBLISHABLE_KEY=
# VITE_SUPABASE_PROJECT_ID=
